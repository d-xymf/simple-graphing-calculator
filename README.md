A simple graphing calculator in Python that plots functions on a 2D graph.

Change the function that is being plotted at line 7
example: `f = lambda n : n*n*n - 2*n` f(x) = x^3 - 2x

![image](graph.png)
